name: Build and Deploy to AWS ECR

on:
  push:
    branches:
      - main  # Trigger when code is pushed to main branch

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AKIA5CBGTT4CPWNT7POI }}
          aws-secret-access-key: ${{ secrets.s6HkFHE9kKSos0nWRXK0KR3eoJFjUYzEwdKeHX+C }}
          aws-region: ${{ secrets.us-east-1 }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build Docker image
        run: |
          IMAGE_URI="${{ secrets.897729142532.dkr.ecr.us-east-1.amazonaws.com/githubopq }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/${{ secrets.ECR_REPOSITORY }}"
          docker build -t $IMAGE_URI .

      - name: Push Docker image to ECR
        run: |
          IMAGE_URI="${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/${{ secrets.ECR_REPOSITORY }}"
          docker push $IMAGE_URI
